<p><% User.all.each do |friend|%>

<%# <%if current_user.id!=friend.id %>
    <%# <%debugger%> 
    <%if friend!=Friend.all%>    
    <br/><%= friend.email %> &nbsp; <%= link_to 'Add as Friend' , friendships_path(friend_id: friend.id), :method => :post %>  
<%end%>
    
    <%end%>
