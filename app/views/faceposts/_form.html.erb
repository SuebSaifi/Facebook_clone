<%= form_with(model: facepost) do |form| %>
  <% if facepost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(facepost.errors.count, "error") %> prohibited this facepost from being saved:</h2>

      <ul>
        <% facepost.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post %>
    <%= form.text_area :post %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
