<section class="section">
<div class="container">
<div class="columns">

<%# <%= render "profile"%>

<%# <%= render "trends"%> 
<%# <%end%>
<%= render 'feed'%>
<%# <%= render 'who-to-follow' %>
</div>
</div>
</section>

